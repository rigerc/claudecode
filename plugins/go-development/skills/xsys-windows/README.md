# Xsys Windows

Use when working with Windows system programming, Windows API calls, registry operations, process management, file operations, cryptography, certificate handling, and Windows-specific functionality using golang.org/x/sys/windows package

## Structure

- `SKILL.md` - Main skill instructions
- `references/` - Detailed documentation loaded as needed
- `scripts/` - Executable code for deterministic operations
- `assets/` - Templates, images, or other resources

## Usage

This skill is automatically discovered by Claude when relevant to the task.
