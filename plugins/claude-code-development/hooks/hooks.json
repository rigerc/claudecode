{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|Write|Edit|MultiEdit|Read|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -qE '(\"/home/|\\\"file_path\\\":\\s*\\\"/home/)' || [[ \"$PWD\" == /home/* ]]; then echo 'ERROR: Operations in /home/ require explicit user permission' >&2; exit 1; fi"
          }
        ]
      }
    ]
  }
}
