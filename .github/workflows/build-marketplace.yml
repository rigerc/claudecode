name: Build Marketplace

on:
  push:
    branches: [ main, master ]
    paths:
      - 'plugins/**'
      - 'scripts/build-marketplace.py'
      - '.github/workflows/build-marketplace.yml'
  pull_request:
    branches: [ main, master ]
    paths:
      - 'plugins/**'
      - 'scripts/build-marketplace.py'
      - '.github/workflows/build-marketplace.yml'
  workflow_dispatch:

jobs:
  build-marketplace:
    runs-on: ubuntu-latest
    name: Build Marketplace & README

    permissions:
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip

    - name: Validate plugin structure
      run: |
        echo "Validating plugin structure..."
        plugins_dir="plugins"

        if [ ! -d "$plugins_dir" ]; then
          echo "❌ plugins directory not found"
          exit 1
        fi

        for plugin_dir in "$plugins_dir"/*/; do
          if [ -d "$plugin_dir" ]; then
            plugin_name=$(basename "$plugin_dir")
            echo "Validating plugin: $plugin_name"

            # Check for required plugin.json
            if [ ! -f "$plugin_dir/.claude-plugin/plugin.json" ]; then
              echo "⚠️  Warning: Missing $plugin_dir/.claude-plugin/plugin.json"
            else
              # Validate JSON syntax
              if ! jq empty "$plugin_dir/.claude-plugin/plugin.json" 2>/dev/null; then
                echo "❌ Invalid JSON in: $plugin_dir/.claude-plugin/plugin.json"
                exit 1
              fi
              echo "✅ Plugin JSON valid for $plugin_name"
            fi

            # Check for README.md
            if [ ! -f "$plugin_dir/README.md" ]; then
              echo "⚠️  Warning: Missing $plugin_dir/README.md"
            else
              echo "✅ README found for $plugin_name"
            fi
          fi
        done

        echo "Plugin structure validation complete!"

    - name: Build marketplace and README
      run: |
        echo "Building marketplace configuration and README..."
        python scripts/build-marketplace.py

        echo "Generated files:"
        echo "- .claude-plugin/marketplace.json"
        echo "- README.md"

        echo ""
        echo "Marketplace preview:"
        cat .claude-plugin/marketplace.json | jq .

    - name: Validate generated marketplace.json
      run: |
        echo "Validating generated marketplace.json..."
        if ! jq empty .claude-plugin/marketplace.json 2>/dev/null; then
          echo "❌ Generated marketplace.json is invalid JSON"
          exit 1
        fi

        # Check required fields
        required_fields=("name" "owner" "plugins")
        for field in "${required_fields[@]}"; do
          if ! jq -e ".$field" .claude-plugin/marketplace.json > /dev/null; then
            echo "❌ Missing required field: $field"
            exit 1
          fi
        done

        # Validate plugin entries
        plugin_count=$(jq '.plugins | length' .claude-plugin/marketplace.json)
        echo "✅ Found $plugin_count plugins in marketplace"

        if [ "$plugin_count" -eq 0 ]; then
          echo "⚠️  Warning: No plugins found in marketplace"
        fi

    - name: Check for changes
      id: changes
      run: |
        if [ -n "$(git status --porcelain)" ]; then
          echo "changed=true" >> $GITHUB_OUTPUT
          echo "Changes detected:"
          git status --porcelain
        else
          echo "changed=false" >> $GITHUB_OUTPUT
          echo "No changes detected"
        fi

    - name: Commit and push changes
      if: steps.changes.outputs.changed == 'true' && github.event_name == 'push'
      run: |
        echo "Committing generated files..."

        # Configure git
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"

        # Add changes
        git add .claude-plugin/marketplace.json README.md

        # Show what will be committed
        echo "Files to be committed:"
        git diff --cached --name-only

        # Commit changes
        git commit -m "🤖 Auto-update marketplace and README from plugin changes

Generated by:
- Python build script: scripts/build-marketplace.py
- Triggered by changes in plugins/ directory
- Workflow: .github/workflows/build-marketplace.yml

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: GitHub Actions <action@github.com>"

        # Push changes
        git push

    - name: Create summary
      run: |
        echo "## 🏗️ Marketplace Build Summary" >> $GITHUB_STEP_SUMMARY

        if [ "${{ steps.changes.outputs.changed }}" == "true" ]; then
          echo "✅ **Status**: Changes detected and committed" >> $GITHUB_STEP_SUMMARY
          echo "📝 **Files Updated**:" >> $GITHUB_STEP_SUMMARY
          echo "- \`.claude-plugin/marketplace.json\`" >> $GITHUB_STEP_SUMMARY
          echo "- \`README.md\`" >> $GITHUB_STEP_SUMMARY
        else
          echo "ℹ️ **Status**: No changes needed" >> $GITHUB_STEP_SUMMARY
        fi

        plugin_count=$(jq '.plugins | length' .claude-plugin/marketplace.json 2>/dev/null || echo "0")
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "📦 **Plugin Count**: $plugin_count" >> $GITHUB_STEP_SUMMARY

        if [ "$plugin_count" -gt 0 ]; then
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "🔌 **Available Plugins**:" >> $GITHUB_STEP_SUMMARY
          jq -r '.plugins[] | "- \(.name)"' .claude-plugin/marketplace.json 2>/dev/null >> $GITHUB_STEP_SUMMARY || echo "No plugins found"
        fi

  # Job to validate the generated files on PRs
  validate-pr:
    runs-on: ubuntu-latest
    name: Validate PR Changes
    if: github.event_name == 'pull_request'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Build marketplace and README
      run: python scripts/build-marketplace.py

    - name: Check if PR includes marketplace updates
      run: |
        echo "Checking if PR should include marketplace updates..."

        # Check if there are changes in plugins directory
        if git diff --name-only origin/${{ github.base_ref }} | grep -q "^plugins/"; then
          echo "🔍 Plugin changes detected in PR"

          # Check if marketplace files are also updated
          if git diff --name-only origin/${{ github.base_ref }} | grep -E "^(README\.md|\.claude-plugin/marketplace\.json)$"; then
            echo "✅ Marketplace files updated in PR"
          else
            echo "⚠️  WARNING: Plugin changes detected but marketplace files not updated!"
            echo "Please run 'python scripts/build-marketplace.py' and commit the changes."
            exit 1
          fi
        else
          echo "ℹ️  No plugin changes in PR"
        fi